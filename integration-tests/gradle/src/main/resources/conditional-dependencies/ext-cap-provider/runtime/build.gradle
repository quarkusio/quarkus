plugins {
    id 'java-library'
    id 'maven-publish'
    id 'io.quarkus.extension'
}

dependencies {
    implementation platform("${quarkusPlatformGroupId}:${quarkusPlatformArtifactId}:${quarkusPlatformVersion}")
}

quarkusExtension {
    deploymentArtifact = "org.acme:ext-cap-provider-deployment:1.0-SNAPSHOT"
    capabilities {
        provides 'test.cap.a'
    }
    conditionalDependencies = ["org.acme:ext-cap-cond::jar:1.0-SNAPSHOT"]
}

publishing {
    publications {
        mavenJave(MavenPublication) {
            groupId = 'org.acme'
            artifactId = 'ext-cap-provider'
            version = '1.0-SNAPSHOT'
            from components.java
        }
    }
}

/**
 * These tests check that filter are correctly applied when using route automatically registered. They are the same as
 * the CORS test from vertx-http, except that the route is registered using @Route.
 */
package io.quarkus.vertx.web.cors;

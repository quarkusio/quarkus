// This is the default
import { str } from '@lit/localize';

export const templates = {
    // Metadata
    'quarkus-netty-meta-description':'Netty is a non-blocking I/O client-server framework. Used by Quarkus as foundation layer.'
};

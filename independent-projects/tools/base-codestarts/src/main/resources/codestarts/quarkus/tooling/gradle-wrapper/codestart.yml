name: gradle-wrapper
type: tooling
output-strategy:
  "gradlew": "executable"
language:
  base:
    data:
      gradle:
        version: 7.4.1
    shared-data:
      buildtool:
        cli: ./gradlew

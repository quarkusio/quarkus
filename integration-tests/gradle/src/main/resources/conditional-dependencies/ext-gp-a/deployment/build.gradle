plugins {
    id 'java-library'
    id 'maven-publish'
}

dependencies {
    implementation platform("${quarkusPlatformGroupId}:${quarkusPlatformArtifactId}:${quarkusPlatformVersion}")
    implementation 'io.quarkus:quarkus-core-deployment'
    implementation project(':ext-gp-a:runtime')
    implementation 'org.acme:ext-gp-b-deployment:1.0-SNAPSHOT'
    implementation 'org.acme:ext-gp-c-deployment:1.0-SNAPSHOT'
}

publishing {
    publications {
        mavenJava(MavenPublication) {
            groupId = 'org.acme'
            artifactId = 'ext-gp-a-deployment'
            version = '1.0-SNAPSHOT'
            from components.java
        }
    }
}

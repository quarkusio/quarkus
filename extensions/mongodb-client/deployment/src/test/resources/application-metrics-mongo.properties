quarkus.mongodb.connection-string=mongodb://127.0.0.1:27018
quarkus.mongodb.metrics.enabled=true

# Metrics go through the Micrometer registry here; OpenTelemetry is only on the test classpath.
# Disable the unused OTel metrics pipeline to avoid the redundancy warning.
quarkus.otel.metrics.enabled=false
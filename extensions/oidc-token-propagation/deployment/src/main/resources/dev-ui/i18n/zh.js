import { str } from '@lit/localize';

export const templates = {
    // Metadata
    'quarkus-resteasy-client-oidc-token-propagation-meta-description':'使用 RESTEasy 客户端过滤器传播传入的 Bearer 访问令牌或从授权代码流获取的令牌作为 HTTP Authorization Bearer 令牌'
};

// This is the default
import { str } from '@lit/localize';

export const templates = {
    // Metadata
    'quarkus-config-yaml-meta-description':'Use YAML to configure your Quarkus application'
};


pluginManagement {
    repositories {
        mavenLocal {
            content {
                includeGroupByRegex 'io.quarkus.*'
                includeGroup 'org.hibernate.orm'
            }
        }
        mavenCentral()
        gradlePluginPortal()
    }
    plugins {
        id 'io.quarkus.application' version "${quarkusPluginVersion}"
    }
}

rootProject.name = 'composite-library-application'
includeBuild 'included-library'

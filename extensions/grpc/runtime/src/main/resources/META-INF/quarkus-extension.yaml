---
artifact: ${project.groupId}:${project.artifactId}:${project.version}
name: "gRPC"
metadata:
  keywords:
    - "gRPC"
  categories:
    - "web"
    - "serialization"
    - "reactive"
  status: "experimental"
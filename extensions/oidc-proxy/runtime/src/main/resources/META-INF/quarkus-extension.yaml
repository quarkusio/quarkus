---
artifact: ${project.groupId}:${project.artifactId}:${project.version}
name: "OpenID Connect Proxy"
metadata:
  keywords:
  - "oauth2"
  - "openid-connect"
  - "oidc"  
  categories:
    - "security"
  status: "experimental"
  config:
  - "quarkus.oidc-proxy."

artifact: ${project.groupId}:${project.artifactId}:${project.version}
name: "Logging Socket"
metadata:
  keywords:
    - "logging"
    - "Socket"
    - "handler"
  guide: "https://quarkus.io/guides/centralized-log-management"
  categories:
    - "core"
  status: "preview"
  config:
    - "quarkus.log.handler.socket."

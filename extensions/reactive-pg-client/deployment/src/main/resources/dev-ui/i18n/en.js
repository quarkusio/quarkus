// This is the default
import { str } from '@lit/localize';

export const templates = {
    // Metadata
    'quarkus-reactive-pg-client-meta-description':'Connect to the PostgreSQL database using the reactive pattern'
};

import { str } from '@lit/localize';

export const templates = {
    // Metadata
    'quarkus-rest-client-oidc-token-propagation-meta-description':'使用 REST 客户端传播传入的 Bearer 访问令牌或从授权代码流获取的令牌作为 HTTP Authorization Bearer 令牌'
};

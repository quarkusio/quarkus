name: gradle-wrapper
type: tooling
output-strategy:
  "gradlew": "executable"
  "gradlew.bat": "executable"
language:
  base:
    data:
      gradle:
        version: 6.9
    shared-data:
      buildtool:
        cli: ./gradlew

        <dependency>
            <groupId>{dep.groupId}</groupId>
            <artifactId>{dep.artifactId}</artifactId>
            {#if dep.version}
            <version>{dep.version}</version>
            {/if}
            {#if scope ne 'compile'}
            <scope>{scope}</scope>
            {/if}
        </dependency>
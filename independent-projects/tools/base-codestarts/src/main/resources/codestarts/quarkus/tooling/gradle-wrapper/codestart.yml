name: gradle-wrapper
type: tooling
output-strategy:
  "gradlew": "executable"
language:
  base:
    data:
      gradle:
        version: 7.4.2
    shared-data:
      buildtool:
        cli: ./gradlew

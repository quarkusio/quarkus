plugins {
    id 'java-library'
    id 'maven-publish'
    id 'io.quarkus.extension'
}

dependencies {
    implementation platform("${quarkusPlatformGroupId}:${quarkusPlatformArtifactId}:${quarkusPlatformVersion}")
    api 'org.acme:ext-gp-i:1.0-SNAPSHOT'
}

quarkusExtension {
    deploymentArtifact = "org.acme:ext-gp-h-deployment:1.0-SNAPSHOT"
    capabilities {
        provides 'cap.gp.y'
    }
}

publishing {
    publications {
        mavenJave(MavenPublication) {
            groupId = 'org.acme'
            artifactId = 'ext-gp-h'
            version = '1.0-SNAPSHOT'
            from components.java
        }
    }
}

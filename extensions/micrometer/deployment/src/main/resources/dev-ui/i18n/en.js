// This is the default
import { str } from '@lit/localize';

export const templates = {
    // Metadata
    'quarkus-micrometer-meta-description':'Instrument the runtime and your application with dimensional metrics using Micrometer.'
};

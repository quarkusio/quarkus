---
artifact: ${project.groupId}:${project.artifactId}:${project.version}
name: "Container Image OpenShift"
metadata:
  keywords:
    - "openshift"
    - "container"
    - "image"
  categories:
    - "cloud"
  status: "stable"

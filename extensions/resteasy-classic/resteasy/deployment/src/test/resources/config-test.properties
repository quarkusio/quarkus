# we add a variable (PREFIX) to make sure that Quarkus will not try to resolve it in dev mode - see https://github.com/quarkusio/quarkus/issues/10320
greeting.message=${PREFIX} from prod mode
%dev.greeting.message=hello from dev mode

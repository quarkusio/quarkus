# Fixed port to detect if container leaks after @QuarkusTest
# If the container isn't stopped, a second run will fail with a port conflict
quarkus.simple-extension.devservices.port=57434
# Fresh port to avoid conflicts with other tests
quarkus.http.test-port=8088

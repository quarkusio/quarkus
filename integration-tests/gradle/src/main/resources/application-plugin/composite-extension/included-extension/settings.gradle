pluginManagement {
    repositories {
        mavenLocal {
            content {
                includeGroupByRegex 'io.quarkus.*'
                includeGroup 'org.hibernate.orm'
            }
        }
        mavenCentral()
        gradlePluginPortal()
    }
    plugins {
        id 'io.quarkus.extension' version "${quarkusPluginVersion}"
        id 'io.quarkus.extension.deployment' version "${quarkusPluginVersion}"
    }
}

rootProject.name = 'included-extension-parent'
include 'runtime', 'deployment'
project(':runtime').name = 'included-extension'
project(':deployment').name = 'included-extension-deployment'

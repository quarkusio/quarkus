quarkus.kubernetes.env.configmaps=configmap,another,without

quarkus.kubernetes.env.using-prefix."PREFIX".for-configmap=configmap
quarkus.kubernetes.env.using-prefix."QUARKUS".for-configmap=another

quarkus.kubernetes.env.using-prefix."ANOTHER".for-configmap=another
quarkus.kubernetes.env.using-prefix."HELLO".for-configmap=configmap

# blank
quarkus.kubernetes.env.using-prefix."".for-configmap=without

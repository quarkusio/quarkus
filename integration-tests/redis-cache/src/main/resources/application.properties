quarkus.redis.hosts=redis://localhost:6379/0
# needed only because we use the same cache for both String and Uni<String> results
quarkus.cache.redis.sunrise-cache.value-type=java.lang.String
io.quarkus.it.cache.redis.SunriseRestClient/mp-rest/url=${test.url}

---
artifact: ${project.groupId}:${project.artifactId}:${project.version}
name: "Container Image Docker"
metadata:
  keywords:
    - "docker"
    - "container"
    - "image"
  guide: "https://quarkus.io/guides/container-image#docker"
  categories:
    - "cloud"
  status: "stable"
  config:
    - "quarkus.docker."

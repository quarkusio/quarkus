# Fixed port so the second run fails with a port conflict if the container isn't properly stopped.
# The specific value is arbitrary but different from the reuse test.
quarkus.simple-extension.devservices.port=56433
# Fresh port to avoid conflicts with other tests
quarkus.http.test-port=8087

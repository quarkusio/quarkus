# Define a datasource so Hibernate ORM can build the PU descriptor (dialect, etc.)
quarkus.datasource.users.db-kind=h2
quarkus.datasource.users.jdbc.url=jdbc:h2:mem:users;DB_CLOSE_DELAY=-1

# Named PU "users"
quarkus.hibernate-orm."users".datasource=users
quarkus.hibernate-orm."users".packages=io.quarkus.hibernate.orm.multiplepersistenceunits.model.annotation.user

# Disable both sides explicitly -> must fail
quarkus.hibernate-orm."users".jdbc.enabled=false
quarkus.hibernate-orm."users".reactive.enabled=false

# Avoid Dev Services noise
quarkus.datasource.devservices.enabled=false
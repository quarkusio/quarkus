---
artifact: ${project.groupId}:${project.artifactId}:${project.version}
name: "Proxy configuration registry"
metadata:
  categories:
  - "web"
  status: "stable"
  guide: "https://quarkus.io/guides/proxy-registry"
  unlisted: true
  config:
  - "quarkus.proxy."

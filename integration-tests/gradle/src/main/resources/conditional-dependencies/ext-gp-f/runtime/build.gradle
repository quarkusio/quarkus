plugins {
    id 'java-library'
    id 'maven-publish'
    id 'io.quarkus.extension'
}

dependencies {
    implementation platform("${quarkusPlatformGroupId}:${quarkusPlatformArtifactId}:${quarkusPlatformVersion}")
    api 'org.acme:ext-gp-g:1.0-SNAPSHOT'
}

quarkusExtension {
    deploymentArtifact = "org.acme:ext-gp-f-deployment:1.0-SNAPSHOT"
    capabilities {
        provides 'cap.gp.x'
    }
}

publishing {
    publications {
        mavenJave(MavenPublication) {
            groupId = 'org.acme'
            artifactId = 'ext-gp-f'
            version = '1.0-SNAPSHOT'
            from components.java
        }
    }
}

# Configure client to use BASIC intelligence mode to avoid connection timeout
# issues on non-Linux platforms where Docker container IPs are not routable
quarkus.infinispan-client.client-intelligence=BASIC

io.quarkus.it.cache.infinispan.SunriseRestClient/mp-rest/url=${test.url}

name: gradle-wrapper
type: tooling
output-strategy:
  "gradlew": "executable"
language:
  base:
    data:
      gradle:
        version: 8.0.2
    shared-data:
      buildtool:
        cli: ./gradlew

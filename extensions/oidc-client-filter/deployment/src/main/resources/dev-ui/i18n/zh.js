import { str } from '@lit/localize';

export const templates = {
    // Metadata
    'quarkus-resteasy-client-oidc-filter-meta-description':'使用 RESTEasy 客户端过滤器，通过 OpenID Connect 客户端获取和刷新访问令牌，并将其作为 HTTP Authorization Bearer 令牌发送'
};

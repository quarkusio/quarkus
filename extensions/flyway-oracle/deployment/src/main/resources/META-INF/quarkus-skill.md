### Usage

- Add this extension alongside `quarkus-flyway` to enable Oracle-specific Flyway features.
- This provides the Oracle dialect for migrations.

### Common Pitfalls

- This is a companion extension — you still need `quarkus-flyway` for core migration functionality.
- See the `quarkus-flyway` skill for migration patterns and configuration.

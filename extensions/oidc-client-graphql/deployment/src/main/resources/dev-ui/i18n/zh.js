import { str } from '@lit/localize';

export const templates = {
    // Metadata
    'quarkus-oidc-client-graphql-meta-description':'使用 GraphQL 客户端，通过 OpenId Connect 客户端获取和刷新访问令牌，并将其作为 HTTP Authorization Bearer 令牌发送'
};

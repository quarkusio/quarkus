name: config-yaml
type: config
language:
  base:
    shared-data:
      config:
        guide: https://quarkus.io/guides/config#yaml
        file-name: application.yml
    dependencies:
      - io.quarkus:quarkus-config-yaml
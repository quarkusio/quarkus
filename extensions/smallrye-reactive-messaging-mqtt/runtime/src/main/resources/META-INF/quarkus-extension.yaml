---
artifact: ${project.groupId}:${project.artifactId}:${project.version}
name: "Messaging - MQTT Connector"
metadata:
  keywords:
  - "mqtt"
  - "reactive-mqtt"
  categories:
  - "messaging"
  status: "preview"
  config:
  - "mp.messaging."
  - "quarkus.messaging."

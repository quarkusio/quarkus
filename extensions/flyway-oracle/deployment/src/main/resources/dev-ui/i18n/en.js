// This is the default
import { str } from '@lit/localize';

export const templates = {
    // Metadata
    'quarkus-flyway-oracle-meta-description':'This extension is added by Quarkus automatically when quarkus-flyway and quarkus-jdbc-oracle are on the classpath'
};

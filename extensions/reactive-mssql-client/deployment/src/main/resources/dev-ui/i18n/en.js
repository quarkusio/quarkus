// This is the default
import { str } from '@lit/localize';

export const templates = {
    // Metadata
    'quarkus-reactive-mssql-client-meta-description':'Connect to the Microsoft SQL Server database using the reactive pattern'
};

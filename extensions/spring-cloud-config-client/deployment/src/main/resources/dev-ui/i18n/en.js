// This is the default
import { str } from '@lit/localize';

export const templates = {
    // Metadata
    'quarkus-spring-cloud-config-client-meta-description':'Use properties from Spring Cloud Config as bootstrap properties sources'
};
